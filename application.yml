eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  info:
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
info:
  author: Matheus Barreto
  mail: matheuspbarreto@hotmail.com.br
server:
  port: ${PORT:6699}
message: Mensagem da configura
